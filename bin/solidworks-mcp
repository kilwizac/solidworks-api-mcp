#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

if [[ -x "target/release/solidworks-mcp-rs" ]]; then
  exec "target/release/solidworks-mcp-rs"
fi

exec cargo run --release --quiet
